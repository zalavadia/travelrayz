/**
 * TRAVELRAYZ — Gallery categories (optional labels for filters)
 * Photos come from Google Sheets via the admin Gallery panel.
 * Keep items empty so the public site only shows real uploaded photos.
 */
const GALLERY_DATA = {
  categories: [],
  items: []
};
